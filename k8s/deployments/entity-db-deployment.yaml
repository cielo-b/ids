apiVersion: apps/v1
kind: Deployment
metadata:
  name: entity-db
  namespace: billme-platform
  labels:
    app: entity-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: entity-db
  template:
    metadata:
      labels:
        app: entity-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: billme_entity
            - name: POSTGRES_PASSWORD
              value: billme_entity_pass
            - name: POSTGRES_DB
              value: billme_entities
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: postgres-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: entity-db-service
  namespace: billme-platform
  labels:
    app: entity-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: entity-db
