apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-db
  namespace: billme-platform
  labels:
    app: employee-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-db
  template:
    metadata:
      labels:
        app: employee-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: billme_employee
            - name: POSTGRES_PASSWORD
              value: billme_employee_pass
            - name: POSTGRES_DB
              value: billme_employees
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: postgres-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: employee-db-service
  namespace: billme-platform
  labels:
    app: employee-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: employee-db
