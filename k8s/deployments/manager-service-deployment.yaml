apiVersion: apps/v1
kind: Deployment
metadata:
  name: manager-service
  namespace: billme-platform
  labels:
    app: manager-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: manager-service
  template:
    metadata:
      labels:
        app: manager-service
    spec:
      containers:
        - name: manager-service
          image: billme-manager-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3005
          envFrom:
            - configMapRef:
                name: manager-service-config
            - secretRef:
                name: manager-service-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/docs
              port: 3005
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/docs
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 5
