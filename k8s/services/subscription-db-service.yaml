apiVersion: v1
kind: Service
metadata:
  name: subscription-db-service
  namespace: billme-platform
  labels:
    app: subscription-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: subscription-db
