apiVersion: v1
kind: Service
metadata:
  name: notification-db-service
  namespace: billme-platform
  labels:
    app: notification-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: notification-db
