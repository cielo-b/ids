apiVersion: v1
kind: Service
metadata:
  name: auth-db-service
  namespace: billme-platform
  labels:
    app: auth-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: auth-db
