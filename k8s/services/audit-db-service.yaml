apiVersion: v1
kind: Service
metadata:
  name: audit-db-service
  namespace: billme-platform
  labels:
    app: audit-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: audit-db
