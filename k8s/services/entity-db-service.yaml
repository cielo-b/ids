apiVersion: v1
kind: Service
metadata:
  name: entity-db-service
  namespace: billme-platform
  labels:
    app: entity-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: entity-db
