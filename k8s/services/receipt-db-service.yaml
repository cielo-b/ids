apiVersion: v1
kind: Service
metadata:
  name: receipt-db-service
  namespace: billme-platform
  labels:
    app: receipt-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: receipt-db
