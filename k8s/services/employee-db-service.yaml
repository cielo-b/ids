apiVersion: v1
kind: Service
metadata:
  name: employee-db-service
  namespace: billme-platform
  labels:
    app: employee-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: employee-db
