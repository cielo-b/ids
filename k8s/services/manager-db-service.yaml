apiVersion: v1
kind: Service
metadata:
  name: manager-db-service
  namespace: billme-platform
  labels:
    app: manager-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: manager-db
